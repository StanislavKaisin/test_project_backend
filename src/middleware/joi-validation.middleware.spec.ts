import { JoiValidationPipe } from './joi-validation.middleware';

describe('JoiValidationMiddleware', () => {
  it('should be defined', () => {
    // expect(new JoiValidationPipe({})).toBeDefined();
  });
});
